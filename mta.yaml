_schema-version: "3.1"
ID: cv-screening-app
description: AI-Powered CV Screening Application
version: 1.0.0

modules:
  - name: cv-screening-app-srv
    type: nodejs
    path: .
    parameters:
      buildpack: nodejs_buildpack
      memory: 256M
      disk-quota: 512M
    properties:
      GROQ_API_KEY: REPLACE_WITH_YOUR_GROQ_API_KEY
